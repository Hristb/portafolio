---
import Logo from "@/components/Logo.astro";
import ThemeSwitcher from "@/components/ThemeSwitcher.astro";
import config from "@/config/config.json";
import menu from "@/config/menu.json";
import { IoSearch } from "react-icons/io5";

export interface ChildNavigationLink {
  name: string;
  url: string;
}

export interface NavigationLink {
  name: string;
  url: string;
  hasChildren?: boolean;
  children?: ChildNavigationLink[];
}

const { main }: { main: NavigationLink[] } = menu;
const { navigation_button, settings } = config;
const { pathname } = Astro.url;

// Icon SVG paths per route — minimal geometric icons
const navIcons: Record<string, string> = {
  "/": `<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="12 2 22 8.5 22 15.5 12 22 2 15.5 2 8.5"/><line x1="12" y1="22" x2="12" y2="15.5"/><polyline points="22 8.5 12 15.5 2 8.5"/></svg>`,
  "/about": `<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="12" cy="8" r="4"/><path d="M4 20c0-4 3.6-7 8-7s8 3 8 7"/></svg>`,
  "/blog": `<svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 2L2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg>`,
};
---

<header class={`header z-30 ${settings.sticky_header && "sticky top-0"}`}>
  <nav class="navbar container">
    <!-- logo -->
    <div class="order-0 min-w-0 flex-shrink">
      <Logo />
    </div>

    <!-- navbar toggler (checkbox) -->
    <input id="nav-toggle" type="checkbox" class="hidden" />
    <label
      for="nav-toggle"
      class="nav-toggle-btn order-3 flex shrink-0 cursor-pointer items-center lg:hidden"
      aria-label="Toggle navigation"
    >
      <!-- Open: 3 staggered lines -->
      <span id="show-button" class="nav-burger block" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
      </span>
      <!-- Close: × -->
      <span id="hide-button" class="nav-close hidden" aria-hidden="true">
        <svg width="22" height="22" viewBox="0 0 22 22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
          <line x1="4" y1="4" x2="18" y2="18"/>
          <line x1="18" y1="4" x2="4" y2="18"/>
        </svg>
      </span>
    </label>
    <!-- /navbar toggler -->

    <!-- Mobile full-screen overlay + Desktop nav -->
    <ul
      id="nav-menu"
      class="navbar-nav order-3 lg:order-1 lg:flex lg:w-auto lg:space-x-2 xl:space-x-8"
    >
      <!-- Mobile overlay header (logo + close) -->
      <li class="nav-overlay-header lg:hidden">
        <span class="nav-overlay-logo">
          <Logo />
        </span>
        <label for="nav-toggle" class="nav-overlay-close" aria-label="Close menu">
          <svg width="22" height="22" viewBox="0 0 22 22" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round">
            <line x1="4" y1="4" x2="18" y2="18"/>
            <line x1="18" y1="4" x2="4" y2="18"/>
          </svg>
        </label>
      </li>

      {
        main.map((item) => (
          <>
            {item.hasChildren ? (
              <li class="nav-item nav-dropdown group relative">
                <span
                  class={`nav-link inline-flex items-center gap-2 ${
                    item.children?.map(({ url }) => url).includes(pathname) ||
                    item.children?.map(({ url }) => `${url}/`).includes(pathname)
                      ? "active"
                      : ""
                  }`}
                >
                  {item.name}
                  <svg class="h-4 w-4 fill-current" viewBox="0 0 20 20">
                    <path d="M9.293 12.95l.707.707L15.657 8l-1.414-1.414L10 10.828 5.757 6.586 4.343 8z" />
                  </svg>
                </span>
                <ul class="nav-dropdown-list hidden group-hover:block lg:invisible lg:absolute lg:block lg:opacity-0 lg:group-hover:visible lg:group-hover:opacity-100">
                  {item.children?.map((child) => (
                    <li class="nav-dropdown-item">
                      <a
                        href={child.url}
                        aria-label={child.name}
                        class={`nav-dropdown-link block ${
                          (pathname === `${child.url}/` || pathname === child.url) && "active"
                        }`}
                      >
                        {child.name}
                      </a>
                    </li>
                  ))}
                </ul>
              </li>
            ) : (
              <li class="nav-item">
                <a
                  href={item.url}
                  class={`nav-link ${
                    (pathname === `${item.url}/` || pathname === item.url) && "active"
                  }`}
                >
                  {/* Icon only shown on mobile overlay */}
                  <span class="nav-link-icon lg:hidden" set:html={navIcons[item.url] ?? ""} />
                  <span class="nav-link-label">{item.name}</span>
                </a>
              </li>
            )}
          </>
        ))
      }

      <!-- Mobile overlay footer -->
      <li class="nav-overlay-footer lg:hidden">
        <ThemeSwitcher className="" />
        {navigation_button.enable && (
          <a
            class="btn btn-outline-primary btn-sm"
            href={navigation_button.link}
            target="_blank"
            rel="noopener noreferrer"
            data-gtm-id="header_cta"
            data-gtm-location="header_mobile"
          >
            {navigation_button.label}
          </a>
        )}
      </li>
    </ul>

    <div class="order-1 ml-auto flex shrink-0 items-center md:order-2 lg:ml-0">
      {
        settings.search && (
          <button
            class="border-border text-text-dark hover:text-primary dark:border-darkmode-border mr-5 inline-block border-r pr-5 text-xl dark:text-white dark:hover:text-darkmode-primary"
            aria-label="search"
            data-search-trigger
          >
            <IoSearch />
          </button>
        )
      }
      <ThemeSwitcher className="mr-5" />
      {
        navigation_button.enable && (
          <a
            class="btn btn-outline-primary btn-sm hidden lg:inline-block"
            href={navigation_button.link}
            target="_blank"
            rel="noopener noreferrer"
            data-gtm-id="header_cta"
            data-gtm-location="header_desktop"
          >
            {navigation_button.label}
          </a>
        )
      }
    </div>
  </nav>
</header>
