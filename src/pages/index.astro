---
import Base from "@/layouts/Base.astro";
import { markdownify } from "@/lib/utils/textConverter";
import type { Feature } from "@/types";
import type { CollectionEntry } from "astro:content";
import { getEntry } from "astro:content";
import { FaCheck } from "react-icons/fa";
import HeroIntro from "@/components/HeroIntro.astro";
import TechStackGrid from "@/components/TechStackGrid";
import ExperienceTimeline from "@/components/ExperienceTimeline";
import GitHubReposSection from "@/components/GitHubReposSection";

interface Homepage {
  features: Feature[];
}

const homepage = (await getEntry(
  "homepage",
  "-index"
)) as CollectionEntry<"homepage">;

const { features } = homepage.data as Homepage;
---

<Base>
  <!-- Hero — minimal intro -->
  <section class="section pt-14 pb-10" id="hero">
    <div class="container">
      <div class="row justify-center">
        <div class="col-12 reveal">
          <HeroIntro />
        </div>
      </div>
    </div>
  </section>

  <!-- Feature sections — flat, editorial cards -->
  {
    features.map((feature, index: number) => (
      <section id={index === 0 ? "trayectoria" : undefined} class="section-sm">
        <div class="container">
          <div class="feature-card">
            <div class="row items-center justify-between">
              <div
                class={`mb:md-0 mb-6 md:col-5 reveal ${index % 2 !== 0 && "md:order-2"}`}
              >
                {index === 0 && <ExperienceTimeline client:visible />}
                {index === 1 && <TechStackGrid client:visible />}
              </div>
              <div
                class={`md:col-7 lg:col-6 reveal delay-1 ${index % 2 !== 0 && "md:order-1"}`}
              >
                <h2
                  set:html={markdownify(feature.title)}
                  class="mb-4 feature-title"
                />
                <p
                  set:html={markdownify(feature.content)}
                  class="mb-6 text-lg feature-desc"
                />
                <ul>
                  {feature.bulletpoints.map((bullet: string) => (
                    <li class="relative mb-3 pl-6">
                      <FaCheck aria-hidden={true} className={"absolute left-0 top-1.5 feature-check"} />
                      <span set:html={markdownify(bullet)} />
                    </li>
                  ))}
                </ul>
                {feature.button.enable && (
                  <a
                    class="btn btn-outline-primary btn-sm mt-5 rounded-full"
                    href={feature.button.link}
                  >
                    {feature.button.label}
                  </a>
                )}
              </div>
            </div>
          </div>
        </div>
      </section>
    ))
  }

  <section class="section">
    <div class="container">
      <GitHubReposSection client:visible />
    </div>
  </section>
</Base>
